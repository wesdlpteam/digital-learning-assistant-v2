/* =============================================================
   03-dashboard-browse-entry.js — INTENTIONALLY EMPTY

   This file used to be a near-verbatim duplicate of
   00-config-state-utils.js, which crashed the page on load with
   "SyntaxError: Identifier 'DATA' has already been declared".

   The kinder additions that were unique to this file (extra YR
   entries, extra YEAR_LEVEL_CHOICES with values -2 and -1) have
   been merged back into 00-config-state-utils.js.

   The dashboard / browse / entry render functions implied by the
   filename actually live in 06-bulk-router-chat.js. The only
   reason this file still exists is to keep the script tag in
   DLA_Studio.html stable. It can be safely deleted (along with
   its <script> tag) at the next refactor.
   ============================================================= */
